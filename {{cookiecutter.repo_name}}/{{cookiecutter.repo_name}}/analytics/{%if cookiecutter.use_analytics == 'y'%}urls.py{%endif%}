from django.urls import path
from . import views

urlpatterns = [
    path("healthcheck", views.healthcheck, name="healthcheck"),
    path("track.js", views.get_track_js, name="get_track_js"),
    path("events", views.get_events, name="events"),
    path("group", views.group, name="group"),
    path("page_view", views.page_view, name="page_view"),
    path("screen_view", views.screen_view, name="screen_view"),
    path("track", views.track, name="track"),
    path("dashboard", views.dashboard, name="dashboard"),
    path("create_event", views.create_event, name="create_event"),
    path("delete_event", views.delete_event, name="delete_event"),
    path("publish_page", views.publish_page, name="publish_page"),
]
