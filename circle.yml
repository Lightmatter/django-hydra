machine:
  python:
    version: 3.6.1
  node:
    version: v8.1.4
  services:
    - postgresql
dependencies:
  override:
    - npm install -g webpack
    - npm install -g yarn
    - pip install cookiecutter
test:
  override:
    - ./test.sh
