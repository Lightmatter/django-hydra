import PropTypes from 'prop-types';
import { Markup } from 'interweave';

const RichText = ({ stream }) => {
    return <Markup content={stream} />;
};

RichText.propTypes = {
    stream: PropTypes.string.isRequired,
};

export default RichText;
