#!/bin/bash
set -e
cd /home/app/webapp
PID=/home/app/gunicorn.pid

if [ -f $PID ]; then rm $PID; fi

exec chpst -u app gunicorn {{cookiecutter.repo_name}}.{{cookiecutter.repo_name}}.asgi:application --bind 0.0.0.0:8000  -k uvicorn.workers.UvicornWorker --pid=$PID
