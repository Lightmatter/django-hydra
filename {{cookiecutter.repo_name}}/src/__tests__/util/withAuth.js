import {
  forwardRequestHeaders,
  loginPageUrl,
  postLoginUrl,
  wrapContextUser,
  wrappedGetInitialProps,
  withAuthRequired,
  withAuth,
  withoutAuth,
} from 'util/withAuth';
//TODO: sets correctly for server or frontend

describe('forwardRequestHeaders', () => {
  it('sets accept-type to json no matter what', () => {});
});
describe('postLoginUrl', () => {});
describe('loginPageUrl', () => {});
describe('wrapContextUser', () => {});
describe('wrappedGetInitialProps', () => {});
describe('withAuth', () => {});
describe('withAuthRequired', () => {});
describe('withoutAuth', () => {});
