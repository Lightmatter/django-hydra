{% from 'forms/field.jinja' import field as f %}
<div class="form-wrapper justify-between my-6">
    {{ errors }}
    {% for field, errors in fields %}
    {{ f(field, errors) }}
    {% endfor %}
    {% for field in hidden_fields %}{{ field }}{% endfor %}
</div>
